.container
  %h1= t('organization.profile.accreditation')
  .progress
    .one.success-color
    .two.success-color
    .three.success-color
    .progress-bar.progress-bar-success{:style => "width: 33%"}

  .well
    %b
      Первый Шаг
    %br
    Выберите тип аккредитации:
    .row
      .col-xs-6.col-sm-3
        = simple_form_for @accreditation, url: wizard_path, method: :put do |f|
          = f.error_notification

          .form-inputs
            = f.input :state, :as => :select, collection: Accreditation.states.find_all{ |key, value| value > 0 && value < 100 },       |
              selected: Accreditation.states[:supplier],                        |
              input_html: { autocomplete: 'off' }                            |

          .form-actions
            = f.button :submit
    %br
    [документы, подтверждения и вот это все]
    %br

  =link_to "Следующее действие", next_wizard_path, :class => "btn-xl btn btn-primary"
